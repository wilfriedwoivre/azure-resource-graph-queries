{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {},
  "variables": {
    "queryName": "list-definitioncount-byscope",
    "queryCode": "policyresources | where type == 'microsoft.authorization/policydefinitions' | extend policyType = tostring(properties.policyType) | where policyType == 'Custom' | project id | extend scope = tostring(split(id, '/providers/Microsoft.Authorization/policyDefinitions/', 0)[0]) | summarize count() by scope | order by count_ desc "
  },
  "resources": [
    {
      "type": "microsoft.resourcegraph/queries",
      "apiVersion": "2018-09-01-preview",
      "name": "[variables('queryName')]",
      "location": "global",
      "properties": {
        "query": "[variables('queryCode')]"
      }
    }
  ]
}